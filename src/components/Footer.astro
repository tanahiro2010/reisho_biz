<footer class="footer">
  <div class="footer__inner">
    <span class="footer__logo">冷笑</span>
    <small class="footer__copy">&copy; 冷笑 All rights reserved.</small>
  </div>
</footer>

<style>
  .footer {
    padding: 24px 0;
    border-top: 1px solid var(--border);
    background: var(--bg);
  }

  .footer__inner {
    max-width: var(--max-width);
    margin: 0 auto;
    padding: 0 40px;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .footer__logo {
    font-family: var(--font-serif);
    font-size: 16px;
    line-height: 1;
    color: var(--text);
  }

  .footer__copy {
    font-size: 11px;
    line-height: 1;
    color: var(--muted);
  }

  @media (max-width: 768px) {
    .footer__inner {
      padding: 0 20px;
      flex-direction: column;
      gap: 8px;
      text-align: center;
    }
  }
</style>
